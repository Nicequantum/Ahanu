/** Garmin Signal VHF AIS output. Raw TCP, receive-only. Not a Cloudflare binding. */
export const GARMIN_SIGNAL_VHF_PORT = 39150;

/** Local JSON gateway datagram port. Convention in this repo, not an env var. */
export const AIS_JSON_UDP_PORT = 39151;
