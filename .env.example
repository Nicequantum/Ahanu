# Pack API origin for the helm client (src/lib/ahanu/pack-client.ts).
# Leave unset for local Vite so Packs hits same-origin.
# CF/prod PWA builds default to the live Worker custom domain when unset.
# https://api.ahanu.dev
# workers.dev fallback: https://ahanu-packs.hombre3536.workers.dev
# VITE_AHANU_PACKS_URL=https://api.ahanu.dev
