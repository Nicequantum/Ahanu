# Pack API origin for the helm client (src/lib/ahanu/pack-client.ts).
# Leave unset for local Vite so Packs hits same-origin.
# CF/prod PWA builds default to the live Worker when unset.
# https://ahanu-packs.hombre3536.workers.dev
# api.ahanu.app is not provisioned.
# VITE_AHANU_PACKS_URL=https://ahanu-packs.hombre3536.workers.dev
