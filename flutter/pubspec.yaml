name: ahanu_domain
description: >
  Shared Dart domain contract for Ahanu. Mirrors src/lib/ahanu so a future
  Flutter helm can share types, geo, and scoring weights with the web PWA.
  TypeScript remains the spec.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dev_dependencies:
  test: ^1.25.8
